# Netlify serves .webmanifest as application/octet-stream, which browsers ignore,
# so the manifest has no effect until the type is corrected here.
/site.webmanifest
  Content-Type: application/manifest+json
