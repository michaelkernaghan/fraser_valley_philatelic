# Never apply line-ending conversion to binaries. Git usually detects these on
# its own, but an autocrlf pass on an image corrupts it silently on checkout.
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
*.pdf  binary
